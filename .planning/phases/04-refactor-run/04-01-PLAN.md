---
phase: 04-refactor-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/
  - frontend/
  - .gitignore
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
user_setup: []

must_haves:
  truths:
    - "Root contains /backend and /frontend directories"
    - "Backend files moved from /src to /backend"
    - "Frontend remains at /frontend"
    - "Gitignore files exist at root and in each project"
  artifacts:
    - path: "backend/Dockerfile"
      provides: "Dockerfile placeholder for Next.js backend"
    - path: "backend/.gitignore"
      provides: "Backend gitignore"
    - path: "frontend/Dockerfile"
      provides: "Dockerfile placeholder for Vue frontend"
    - path: "frontend/.gitignore"
      provides: "Frontend gitignore"
    - path: ".gitignore"
      provides: "Root gitignore"
  key_links:
    - from: "backend"
      to: "prisma"
      via: "moved with src"
    - from: "frontend"
      to: "backend"
      via: "API calls"
---

<objective>
Refactor project structure - move /src to /backend, keep /frontend, add gitignore files.

Purpose: Establish the /backend and /frontend directory structure that Docker will build from.
Output: Restructured project with gitignore files.
</objective>

<execution_context>
@C:/Users/tnolv/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/tnolv/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-refactor-run/04-CONTEXT.md
@.planning/phases/04-refactor-run/04-RESEARCH.md

# From prior phases
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/02-advanced-features/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move /src to /backend</name>
  <files>backend/</files>
  <action>
Move the /src directory contents to /backend:
1. Create /backend directory
2. Move all contents from /src to /backend (files, subdirectories)
3. Keep /src empty or remove if no longer needed
4. Verify: backend/ contains what was in src/

NOTE: Do NOT move:
- /frontend (stays in place)
- /.planning (planning metadata)
- /prisma (schema stays at root, used by backend)
  </action>
  <verify>
    <automated>ls backend/ | test -d app</automated>
  </verify>
  <done>/src moved to /backend, /backend/app/api/... exists</done>
</task>

<task type="auto">
  <name>Task 2: Verify frontend unchanged</name>
  <files>frontend/</files>
  <action>
Verify /frontend exists and remains at root (no changes needed).
Frontend should already exist from prior phase.
Verify: frontend/src, frontend/package.json present
  </action>
  <verify>
    <automated>test -f frontend/package.json && test -d frontend/src</automated>
  </verify>
  <done>Frontend structure verified at /frontend</done>
</task>

<task type="auto">
  <name>Task 3: Create gitignore files</name>
  <files>.gitignore, backend/.gitignore, frontend/.gitignore</files>
  <action>
Create three .gitignore files:

1. Root .gitignore:
```
node_modules/
.env*
.next/
dist/
.DS_Store
*.log
```

2. backend/.gitignore:
```
.prisma/
coverage/
.next/
```

3. frontend/.gitignore:
```
dist/
eslint.cache/
```

Use the patterns from RESEARCH.md (lines 246-269).
  </action>
  <verify>
    <automated>test -f .gitignore -a -f backend/.gitignore -a -f frontend/.gitignore</automated>
  </verify>
  <done>Three .gitignore files created with proper patterns</done>
</task>

</tasks>

<verification>
- [ ] /src moved to /backend
- [ ] /frontend at root verified
- [ ] Three .gitignore files exist
</verification>

<success_criteria>
Project restructured with backend/frontend directories and gitignore files in place.
</success_criteria>

<output>
After completion, create `.planning/phases/04-refactor-run/04-01-SUMMARY.md`
</output>