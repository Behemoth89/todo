---
phase: 04-refactor-run
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - docker-compose.yml
  - .env
autonomous: true
requirements:
  - D-06
  - D-07
  - D-08
user_setup: []

must_haves:
  truths:
    - "Docker Compose orchestrates all services"
    - "Database runs in container"
    - "All services can start with docker-compose up"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Orchestration of backend, frontend, postgres"
      must_include:
        - "services:"
        - "backend"
        - "frontend"
        - "postgres"
        - "depends_on"
        - "healthcheck"
    - path: ".env"
      provides: "Environment variables template"
      must_include:
        - "DATABASE_URL="
        - "POSTGRES_"
---

<objective>
Create Docker Compose orchestration for all services.

Purpose: Orchestrate backend, frontend, and postgres to run together.
Output: docker-compose.yml and .env template.
</objective>

<execution_context>
@C:/Users/tnolv/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/tnolv/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-refactor-run/04-CONTEXT.md
@.planning/phases/04-refactor-run/04-RESEARCH.md

# Docker Compose patterns from RESEARCH.md lines 193-244
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env template</name>
  <files>.env</files>
  <action>
Create /.env file with database configuration:

```
# Database
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/family_chores
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=family_chores
```

This will be used by docker-compose.yml to set environment variables.
  </action>
  <verify>
    <automated>test -f .env</automated>
  </verify>
  <done>.env template created with database vars</done>
</task>

<task type="auto">
  <name>Task 2: Create docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
Create /docker-compose.yml:

```yaml
version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/family_chores
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    environment:
      - VITE_API_URL=http://backend:3000
    depends_on:
      - backend
    networks:
      - app-network
    restart: unless-stopped

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: family_chores
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d family_chores"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - app-network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  app-network:
    driver: bridge
```

Based on RESEARCH.md lines 193-244. Includes healthcheck for postgres.
  </action>
  <verify>
    <automated>test -f docker-compose.yml</automated>
  </verify>
  <done>Docker Compose orchestration created</done>
</task>

</tasks>

<verification>
- [ ] .env template exists
- [ ] docker-compose.yml orchestrates 3 services
- [ ] Healthcheck on postgres
- [ ] Networks configured
</verification>

<success_criteria>
Full stack runs via `docker-compose up --build`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-refactor-run/04-04-SUMMARY.md`
</output>