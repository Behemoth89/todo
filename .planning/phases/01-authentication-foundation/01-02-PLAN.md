---
phase: 01-authentication-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - FAM-01
  - FAM-02
  - FAM-03

must_haves:
  truths:
    - User can register with email/password and create or join a family via UI
    - User can log in and maintain authenticated session
    - User can log out
    - Family invite code can be generated and shared
  artifacts:
    - path: frontend/src/stores/auth.ts
      provides: Pinia store for auth state
      min_lines: 50
    - path: frontend/src/components/LoginForm.vue
      provides: Login form with email/password
    - path: frontend/src/components/RegisterForm.vue
      provides: Register form with family creation/selection
    - path: frontend/src/components/FamilySelector.vue
      provides: Family selection during registration
    - path: frontend/src/api/client.ts
      provides: HTTP client with token interceptors
    - path: frontend/src/App.vue
      provides: Main app with dark theme
  key_links:
    - from: frontend/src/api/client.ts
      to: http://localhost:3000/api/v1/auth/*
      via: fetch with credentials
      pattern: credentials: include
    - from: frontend/src/stores/auth.ts
      to: frontend/src/api/client.ts
      via: import and use
      pattern: import.*client
---

<objective>
Complete missing frontend components for Phase 1 authentication foundation. Build Vue 3 + Pinia frontend with login/register forms, family management, and dark theme.
</objective>

<context>
@.planning/phases/01-authentication-foundation/01-01-SUMMARY.md
@backend/src/routes/auth.ts
@backend/src/schemas/auth.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/src/schemas/auth.ts:
```typescript
export const loginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
});

export const registerNewFamilySchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
  familyName: z.string().min(1),
});

export const registerJoinFamilySchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
  inviteCode: z.string().min(8),
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vue 3 frontend project</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/vite.config.ts, frontend/index.html</files>
  <action>
    Create Vue 3 frontend project with Vite, TypeScript (strict mode), and Pinia.
    
    Steps:
    1. Initialize npm project with required dependencies:
       - vue, vue-router, pinia
       - @vitejs/plugin-vue
       - typescript, vite
    2. Create vite.config.ts with Vue plugin
    3. Create tsconfig.json with strict mode
    4. Create index.html entry point
  </action>
  <verify>
    <automated>cd frontend && npm install 2>&1 | head -20</automated>
  </verify>
  <done>Vue 3 project initialized with package.json and config files</done>
</task>

<task type="auto">
  <name>Task 2: Set up TailwindCSS dark theme</name>
  <files>frontend/tailwind.config.js, frontend/src/style.css</files>
  <action>
    Configure TailwindCSS with dark theme support.
    
    1. Create tailwind.config.js with darkMode: 'class'
    2. Create src/style.css with @tailwind directives
    3. Ensure dark theme is default
  </action>
  <verify>
    <automated>grep -l "darkMode" frontend/tailwind.config.js</automated>
  </verify>
  <done>TailwindCSS configured with dark theme</done>
</task>

<task type="auto">
  <name>Task 3: Create HTTP client with token interceptors</name>
  <files>frontend/src/api/client.ts</files>
  <action>
    Build HTTP client with automatic token handling.
    
    Features:
    - Base URL configurable via env (VITE_API_URL)
    - Automatic include credentials for cookies
    - Token interceptor reads accessToken from cookie (credentials: include)
    - Error handling for 401 responses (trigger logout or refresh)
    - Request/response logging in dev mode
    
    Reference: backend returns accessToken in HttpOnly cookie, so credentials: 'include' is required.
  </action>
  <verify>
    <automated>grep -c "credentials" frontend/src/api/client.ts</automated>
  </verify>
  <done>HTTP client with token interceptors implemented</done>
</task>

<task type="auto">
  <name>Task 4: Create Pinia auth store</name>
  <files>frontend/src/stores/auth.ts</files>
  <action>
    Create Pinia store for authentication state management.
    
    State:
    - user: { id, email, familyId } | null
    - isAuthenticated: boolean
    - familyName: string
    
    Actions:
    - login(email, password) -> calls /api/v1/auth/login
    - registerNewFamily(email, password, familyName) -> calls /api/v1/auth/register/new-family
    - registerJoinFamily(email, password, inviteCode) -> calls /api/v1/auth/register/join-family
    - logout() -> calls /api/v1/auth/logout
    - checkAuth() -> checks if user is authenticated
    
    Reference: backend/src/routes/auth.ts for exact endpoints and response format.
  </action>
  <verify>
    <automated>grep -c "export.*useAuthStore" frontend/src/stores/auth.ts</automated>
  </verify>
  <done>Pinia auth store with login/register/logout actions</done>
</task>

<task type="auto">
  <name>Task 5: Create login and register forms</name>
  <files>frontend/src/components/LoginForm.vue, frontend/src/components/RegisterForm.vue, frontend/src/components/FamilySelector.vue</files>
  <action>
    Create Vue components for authentication UI.
    
    LoginForm.vue:
    - Email input (email type)
    - Password input (password type)
    - Submit button
    - Error message display
    - Validates with Zod before submit
    
    RegisterForm.vue:
    - Toggle between "Create Family" and "Join Family" modes
    - Email and password inputs
    - FamilySelector component for mode selection
    - Invite code input (shown when joining)
    - Submit with validation
    
    FamilySelector.vue:
    - Radio/toggle: "Create new family" vs "Join existing family"
    - Family name input (for create)
    - Invite code input (for join)
  </action>
  <verify>
    <automated>ls frontend/src/components/*.vue</automated>
  </verify>
  <done>Login and register forms with family selection UI</done>
</task>

<task type="auto">
  <name>Task 6: Wire components with Vue Router</name>
  <files>frontend/src/App.vue, frontend/src/router/index.ts</files>
  <action>
    Connect all frontend components with routing and state.
    
    App.vue:
    - Apply dark theme class to root
    - Show LoginForm or RegisterForm based on route
    - Show authenticated view when logged in
    - Navigation: Login, Register, Logout buttons
    
    router/index.ts:
    - / -> login (or redirect to /dashboard if authenticated)
    - /register -> registration form
    - /dashboard -> authenticated view (requires auth)
  </action>
  <verify>
    <automated>grep -c "dark" frontend/src/App.vue</automated>
  </verify>
  <done>Frontend wired with Vue Router and dark theme</done>
</task>

</tasks>

<verification>
- Vue dev server starts without errors
- Login form submits to /api/v1/auth/login
- Register form creates family or joins via invite code
- Dark theme applied by default
- Pinia store manages auth state
</verification>

<success_criteria>
User can register with email/password and create or join family via UI.
User can log in and maintain authenticated session.
User can log out.
Family invite code can be generated and shared.
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-foundation/{phase}-{plan}-SUMMARY.md`
</output>